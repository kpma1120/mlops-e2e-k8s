apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: df988fxbot4hsf   # same as uid in dashboard JSON
    url: http://prometheus:9090
    isDefault: true
    editable: false
